# Netegram

Клиент Telegram для iOS с собственным брендингом и расширенными настройками оформления.

---

## Возможности

### Настройки → Netegram

Отдельный раздел в настройках, сразу после «Мой профиль».

### Фейк-активность

**Показывать активность** — выбранные чаты постоянно видят, что вы печатаете, записываете голосовое, отправляете фото и так далее. Действие выбирается одно: сервер принимает по одному на чат. Работает, пока приложение живо, даже если чат ни разу не открывали.

**Читать без открытия** — сообщения из выбранных чатов помечаются прочитанными сразу, как приходят.

Режим призрака сильнее обоих: если то же самое действие скрыто там, наружу оно не уйдёт.

### Автоформат

Стиль, которым набирается весь текст: жирный, курсив, подчёркнутый, зачёркнутый, моноширинный, спойлер. Стили складываются. Применяются прямо в поле ввода, а не только в отправленном сообщении; форматирование, поставленное вручную, остаётся как есть.

### Liquid Glass

Четыре поверхности, каждая со своим переключателем: **сообщения**, **кнопки ботов**, **поле ввода**, **нижняя панель**.

### Режим призрака

Что о вас видят другие: набор текста, запись голосового, отчёты о прочтении, онлайн, просмотры историй, скриншоты. Плюс анти-удаление и анти-редактирование — сообщение, которое пытались забрать назад, остаётся с красной корзиной рядом. Ответ на такое сообщение уходит цитатой в теле: у собеседника этого сообщения уже нет, и обычная ссылка на ответ ему ничего не покажет.

### Локальные функции

Премиум, баланс звёзд и юзернеймы, которые видит только этот клиент. На сервере ничего не меняется.

### Перенос настроек

Экспорт, импорт и сброс всех настроек Netegram одним JSON-файлом.

### Иконка приложения

Основная иконка — Netegram. Оригинальная иконка Telegram доступна как альтернативная в разделе «Оформление → Иконка приложения».

---

## Как хранятся настройки

Всё, что принадлежит форку, лежит в `NGStore` — общем хранилище в контейнере App Group, а не в `UserDefaults` приложения.

Так пришлось сделать потому, что Telegram — это не один процесс. Расширение уведомлений применяет апдейты и удаления в общий Postbox, а `UserDefaults.standard` внутри него — отдельное хранилище, всегда пустое. Анти-удаление, прочитанное оттуда, всегда было выключено: сообщение, удалённое при свёрнутом приложении, удалялось по-настоящему. Это и выглядело как «настройки сбрасываются сами через несколько часов».

Источник правды — атомарный JSON-файл рядом с общими настройками; сами настройки остаются зеркалом для совместимости. Если зеркало опустело, а файл цел, значения возвращаются на место при следующем запуске. Список удалённых сообщений и история правок живут в отдельных файлах: они дописываются на каждое входящее удаление и не должны делить документ с переключателями.

---

## Состояние

| Возможность | Состояние |
|---|---|
| Ребрендинг интерфейса | готово |
| Иконка приложения | готово |
| Раздел настроек Netegram | готово |
| Общее хранилище настроек (NGStore) | готово, не проверено на устройстве |
| Liquid Glass: сообщения, кнопки ботов, поле ввода, нижняя панель | готово |
| Режим призрака | готово |
| Анти-удаление и анти-редактирование | готово |
| Цитата удалённого сообщения в ответе | готово, не проверено на устройстве |
| Автоформат | готово, не проверено на устройстве |
| Фейк-активность | готово, не проверено на устройстве |
| Фейк «онлайн» для выбранных | не реализуемо клиентом, см. ниже |
| Локальные функции | готово |
| Перенос настроек | готово |

**Про фейк «онлайн».** Онлайн-статус в Telegram общий, не по собеседникам: клиент шлёт `account.updateStatus`, и его видят все, кому статус вообще открыт. Показать себя онлайн одному и офлайн другому можно только настройками приватности «Последняя активность» — это реальная серверная настройка аккаунта, она уже есть в самом Telegram, и подделкой не является. Глобальные «всегда онлайн» и «скрыть онлайн» есть в режиме призрака.

Жидкое стекло использует `UIGlassEffect` из iOS 26. На более ранних версиях включается запасной путь — обычное полупрозрачное размытие.


Переводы интерфейса на языки кроме английского приходят с сервера в момент запуска, поэтому часть строк там остаётся неизменённой.

---

## Сборка

Требуется macOS 26 и Xcode 26.2 (см. `versions.json`). Собрать под Windows или Linux нельзя.

### 1. Свой api_id

Получите на [my.telegram.org/apps](https://my.telegram.org/apps).

### 2. Конфигурация

Файлы с ключами и данными подписи в репозиторий не входят — они перечислены в `.gitignore`. После клона создайте `build-system/my-configuration.json`:

```json
{
	"bundle_id": "org.<случайная строка>.netegram",
	"api_id": "<ваш api_id>",
	"api_hash": "<ваш api_hash>",
	"team_id": "<Apple Team ID>",
	"app_center_id": "0",
	"is_internal_build": "false",
	"is_appstore_build": "false",
	"appstore_id": "0",
	"app_specific_url_scheme": "tg",
	"premium_iap_product_id": "",
	"enable_siri": false,
	"enable_icloud": false
}
```

Случайная строка: `openssl rand -hex 8`.
Team ID: `Keychain Access` → `Certificates` → сертификат `Apple Development` → `Organizational Unit`.

Для переменных Bazel скопируйте `build-system/example-configuration/variables.bzl.example` в `variables.bzl` и заполните.

### 3. Проект Xcode

```bash
python3 build-system/Make/Make.py \
    --cacheDir="$HOME/.cache/netegram-bazel" \
    generateProject \
    --configurationPath=build-system/my-configuration.json \
    --xcodeManagedCodesigning
```

Для сборки только под симулятор подпись не нужна — добавьте `--disableProvisioningProfiles`.

### 4. IPA

```bash
python3 build-system/Make/Make.py \
    --cacheDir="$HOME/.cache/netegram-bazel" \
    build \
    --configurationPath=build-system/my-configuration.json \
    --codesigningInformationPath=<папка с профилями> \
    --buildNumber=100001 \
    --configuration=release_arm64
```

### Кеш

В `.bazelrc` включён дисковый кеш в `~/.cache/netegram-bazel`. Он переживает `bazel clean` и свежий клон, поэтому прерванная сборка продолжается с места остановки. Кеш растёт неограниченно — изредка чистите папку вручную.

### Сборка на GitHub Actions

Workflow `.github/workflows/build.yml` собирает неподписанный IPA на раннере `macos-26` и складывает его в артефакты сборки. Перед первым запуском задайте секреты `API_ID` и `API_HASH` в `Settings → Secrets and variables → Actions`.

---

## Известные проблемы

**Xcode зависает на `build-request.json not updated yet`** — отмените сборку и запустите заново.

**`Telegram_xcodeproj: no such package`** после перезагрузки — перегенерируйте проект.

---

## Лицензия

Проект основан на [Telegram-iOS](https://github.com/TelegramMessenger/Telegram-iOS) и распространяется на тех же условиях: исходный код должен оставаться открытым.

Требования к сторонним клиентам: использовать собственный `api_id`, не выдавать приложение за официальное, соблюдать [security guidelines](https://core.telegram.org/mtproto/security_guidelines).

Оригинальная иконка Telegram, доступная как альтернативная, остаётся товарным знаком Telegram — для публикации в App Store её лучше убрать.
